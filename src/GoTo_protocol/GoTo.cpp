/*
 * Copyright (C) 2006-2020 Istituto Italiano di Tecnologia (IIT)
 * All rights reserved.
 *
 * This software may be modified and distributed under the terms of the
 * BSD-3-Clause license. See the accompanying LICENSE file for details.
 */

// Autogenerated by Thrift Compiler (0.12.0-yarped)
//
// This is an automatically generated file.
// It could get re-generated if the ALLOW_IDL_GENERATION flag is on.

#include <GoTo.h>

#include <yarp/os/idl/WireTypes.h>

class GoTo_goTo_helper :
        public yarp::os::Portable
{
public:
    explicit GoTo_goTo_helper() = default;
    explicit GoTo_goTo_helper(const std::string& destination);
    bool write(yarp::os::ConnectionWriter& connection) const override;
    bool read(yarp::os::ConnectionReader& connection) override;

    std::string m_destination;
};

GoTo_goTo_helper::GoTo_goTo_helper(const std::string& destination) :
        m_destination{destination}
{
}

bool GoTo_goTo_helper::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(2)) {
        return false;
    }
    if (!writer.writeTag("goTo", 1, 1)) {
        return false;
    }
    if (!writer.writeString(m_destination)) {
        return false;
    }
    return true;
}

bool GoTo_goTo_helper::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListReturn()) {
        return false;
    }
    return true;
}

class GoTo_getStatus_helper :
        public yarp::os::Portable
{
public:
    explicit GoTo_getStatus_helper() = default;
    explicit GoTo_getStatus_helper(const std::string& destination);
    bool write(yarp::os::ConnectionWriter& connection) const override;
    bool read(yarp::os::ConnectionReader& connection) override;

    std::string m_destination;

    GoToStatus m_return_helper {};
};

GoTo_getStatus_helper::GoTo_getStatus_helper(const std::string& destination) :
        m_destination{destination}
{
}

bool GoTo_getStatus_helper::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(2)) {
        return false;
    }
    if (!writer.writeTag("getStatus", 1, 1)) {
        return false;
    }
    if (!writer.writeString(m_destination)) {
        return false;
    }
    return true;
}

bool GoTo_getStatus_helper::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListReturn()) {
        return false;
    }
    int32_t ecast0;
    GoToStatusVocab cvrt1;
    if (!reader.readEnum(ecast0, cvrt1)) {
        reader.fail();
        return false;
    } else {
        m_return_helper = static_cast<GoToStatus>(ecast0);
    }
    return true;
}

class GoTo_halt_helper :
        public yarp::os::Portable
{
public:
    explicit GoTo_halt_helper() = default;
    explicit GoTo_halt_helper(const std::string& destination);
    bool write(yarp::os::ConnectionWriter& connection) const override;
    bool read(yarp::os::ConnectionReader& connection) override;

    std::string m_destination;
};

GoTo_halt_helper::GoTo_halt_helper(const std::string& destination) :
        m_destination{destination}
{
}

bool GoTo_halt_helper::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(2)) {
        return false;
    }
    if (!writer.writeTag("halt", 1, 1)) {
        return false;
    }
    if (!writer.writeString(m_destination)) {
        return false;
    }
    return true;
}

bool GoTo_halt_helper::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListReturn()) {
        return false;
    }
    return true;
}

class GoTo_isAtLocation_helper :
        public yarp::os::Portable
{
public:
    explicit GoTo_isAtLocation_helper() = default;
    explicit GoTo_isAtLocation_helper(const std::string& destination);
    bool write(yarp::os::ConnectionWriter& connection) const override;
    bool read(yarp::os::ConnectionReader& connection) override;

    std::string m_destination;

    bool m_return_helper {};
};

GoTo_isAtLocation_helper::GoTo_isAtLocation_helper(const std::string& destination) :
        m_destination{destination}
{
}

bool GoTo_isAtLocation_helper::write(yarp::os::ConnectionWriter& connection) const
{
    yarp::os::idl::WireWriter writer(connection);
    if (!writer.writeListHeader(2)) {
        return false;
    }
    if (!writer.writeTag("isAtLocation", 1, 1)) {
        return false;
    }
    if (!writer.writeString(m_destination)) {
        return false;
    }
    return true;
}

bool GoTo_isAtLocation_helper::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    if (!reader.readListReturn()) {
        return false;
    }
    if (!reader.readBool(m_return_helper)) {
        reader.fail();
        return false;
    }
    return true;
}

// Constructor
GoTo::GoTo()
{
    yarp().setOwner(*this);
}

void GoTo::goTo(const std::string& destination)
{
    GoTo_goTo_helper helper{destination};
    if (!yarp().canWrite()) {
        yError("Missing server method '%s'?", "void GoTo::goTo(const std::string& destination)");
    }
    yarp().write(helper, helper);
}

GoToStatus GoTo::getStatus(const std::string& destination)
{
    GoTo_getStatus_helper helper{destination};
    if (!yarp().canWrite()) {
        yError("Missing server method '%s'?", "GoToStatus GoTo::getStatus(const std::string& destination)");
    }
    bool ok = yarp().write(helper, helper);
    return ok ? helper.m_return_helper : GoToStatus{};
}

void GoTo::halt(const std::string& destination)
{
    GoTo_halt_helper helper{destination};
    if (!yarp().canWrite()) {
        yError("Missing server method '%s'?", "void GoTo::halt(const std::string& destination)");
    }
    yarp().write(helper, helper);
}

bool GoTo::isAtLocation(const std::string& destination)
{
    GoTo_isAtLocation_helper helper{destination};
    if (!yarp().canWrite()) {
        yError("Missing server method '%s'?", "bool GoTo::isAtLocation(const std::string& destination)");
    }
    bool ok = yarp().write(helper, helper);
    return ok ? helper.m_return_helper : bool{};
}

// help method
std::vector<std::string> GoTo::help(const std::string& functionName)
{
    bool showAll = (functionName == "--all");
    std::vector<std::string> helpString;
    if (showAll) {
        helpString.emplace_back("*** Available commands:");
        helpString.emplace_back("goTo");
        helpString.emplace_back("getStatus");
        helpString.emplace_back("halt");
        helpString.emplace_back("isAtLocation");
        helpString.emplace_back("help");
    } else {
        if (functionName == "goTo") {
            helpString.emplace_back("void goTo(const std::string& destination) ");
        }
        if (functionName == "getStatus") {
            helpString.emplace_back("GoToStatus getStatus(const std::string& destination) ");
        }
        if (functionName == "halt") {
            helpString.emplace_back("void halt(const std::string& destination) ");
        }
        if (functionName == "isAtLocation") {
            helpString.emplace_back("bool isAtLocation(const std::string& destination) ");
        }
        if (functionName == "help") {
            helpString.emplace_back("std::vector<std::string> help(const std::string& functionName = \"--all\")");
            helpString.emplace_back("Return list of available commands, or help message for a specific function");
            helpString.emplace_back("@param functionName name of command for which to get a detailed description. If none or '--all' is provided, print list of available commands");
            helpString.emplace_back("@return list of strings (one string per line)");
        }
    }
    if (helpString.empty()) {
        helpString.emplace_back("Command not found");
    }
    return helpString;
}

// read from ConnectionReader
bool GoTo::read(yarp::os::ConnectionReader& connection)
{
    yarp::os::idl::WireReader reader(connection);
    reader.expectAccept();
    if (!reader.readListHeader()) {
        reader.fail();
        return false;
    }

    std::string tag = reader.readTag();
    bool direct = (tag == "__direct__");
    if (direct) {
        tag = reader.readTag();
    }
    while (!reader.isError()) {
        if (tag == "goTo") {
            std::string destination;
            if (!reader.readString(destination)) {
                reader.fail();
                return false;
            }
            goTo(destination);
            yarp::os::idl::WireWriter writer(reader);
            if (!writer.isNull()) {
                if (!writer.writeListHeader(0)) {
                    return false;
                }
            }
            reader.accept();
            return true;
        }
        if (tag == "getStatus") {
            std::string destination;
            if (!reader.readString(destination)) {
                reader.fail();
                return false;
            }
            GoTo_getStatus_helper helper{destination};
            helper.m_return_helper = getStatus(destination);
            yarp::os::idl::WireWriter writer(reader);
            if (!writer.isNull()) {
                if (!writer.writeListHeader(1)) {
                    return false;
                }
                if (!writer.writeI32(static_cast<int32_t>(helper.m_return_helper))) {
                    return false;
                }
            }
            reader.accept();
            return true;
        }
        if (tag == "halt") {
            std::string destination;
            if (!reader.readString(destination)) {
                reader.fail();
                return false;
            }
            halt(destination);
            yarp::os::idl::WireWriter writer(reader);
            if (!writer.isNull()) {
                if (!writer.writeListHeader(0)) {
                    return false;
                }
            }
            reader.accept();
            return true;
        }
        if (tag == "isAtLocation") {
            std::string destination;
            if (!reader.readString(destination)) {
                reader.fail();
                return false;
            }
            GoTo_isAtLocation_helper helper{destination};
            helper.m_return_helper = isAtLocation(destination);
            yarp::os::idl::WireWriter writer(reader);
            if (!writer.isNull()) {
                if (!writer.writeListHeader(1)) {
                    return false;
                }
                if (!writer.writeBool(helper.m_return_helper)) {
                    return false;
                }
            }
            reader.accept();
            return true;
        }
        if (tag == "help") {
            std::string functionName;
            if (!reader.readString(functionName)) {
                functionName = "--all";
            }
            auto help_strings = help(functionName);
            yarp::os::idl::WireWriter writer(reader);
            if (!writer.isNull()) {
                if (!writer.writeListHeader(2)) {
                    return false;
                }
                if (!writer.writeTag("many", 1, 0)) {
                    return false;
                }
                if (!writer.writeListBegin(BOTTLE_TAG_INT32, static_cast<uint32_t>(help_strings.size()))) {
                    return false;
                }
                for (const auto& help_string : help_strings) {
                    if (!writer.writeString(help_string)) {
                        return false;
                    }
                }
                if (!writer.writeListEnd()) {
                    return false;
                }
            }
            reader.accept();
            return true;
        }
        if (reader.noMore()) {
            reader.fail();
            return false;
        }
        std::string next_tag = reader.readTag();
        if (next_tag == "") {
            break;
        }
        tag.append("_").append(next_tag);
    }
    return false;
}
